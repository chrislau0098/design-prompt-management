---
style_name: "Instrument Dark · Electric Blue"
pattern_name: "Data Campaign Report"
description: Generates scrollable data narrative pages in instrument-dark electric blue style for any data campaign report scene — annual recap, quarterly summary, growth review, or year-end report.
---

## 1. Brand & Style

Mood: **Cool · Precise · Instrumental · Restrained**

Proposition: A dark instrument panel where numbers are precision readouts — white display figures dominate, electric blue signals direction and state, drama comes from structural light rather than decoration. Fintech restraint: deep negative space, engineered geometric type, light as material.

## 2. Colors

Mode: dark. Color space: OKLCH.

```css
:root, .dark {
  --brand-hue: 262;
  --background:    oklch(0.12 0.015 262);
  --surface-l1:    oklch(0.12 0.015 262);
  --surface-l2:    oklch(0.165 0.018 262);
  --surface-l3:    oklch(0.23 0.022 262);
  --foreground:    oklch(0.96 0.008 255);
  --foreground-2:  oklch(0.78 0.010 255);
  --foreground-3:  oklch(0.58 0.012 255);
  --border:        oklch(1 0 0 / 0.06);
  --border-strong: oklch(1 0 0 / 0.12);
  --primary:       oklch(0.62 0.18 var(--brand-hue));
  --primary-hl:    oklch(0.70 0.16 var(--brand-hue));
  --primary-glow:  oklch(0.70 0.16 var(--brand-hue) / 0.18);
  --chart-1:       var(--primary);
  --chart-2:       oklch(0.545 0.150 var(--brand-hue));
  --chart-3:       oklch(0.470 0.120 var(--brand-hue));
  --chart-4:       oklch(0.395 0.090 var(--brand-hue));
  --chart-5:       oklch(0.320 0.060 var(--brand-hue));
  --chart-hover:   oklch(0.165 0.018 262 / 0.86);
  --quote-bg:      oklch(0.12 0.015 262);
  --outro-bg:      oklch(0.12 0.015 262);
  --glow-ink:      oklch(0.20 0.020 262 / 0.18);
}
```

Color rules:
- Focal numbers use `--foreground` (white). `--primary` (electric blue) is a signal accent only — used for: unit text, directional arrows, structural light, chart-1, delta-up state, and one accent element per section. **Canonical example**: a Hero metric `48.9%` renders as white digits `48.9` with the `%` sign in `--primary` (electric blue). The unit / signal carries the accent, not the digits.
- Complex charts use the ordered Primary blue ramp: chart-1 starts at `--primary`, then steps through darker same-hue blue values. No green, orange, rainbow, or library-default categorical palette.
- Single-series charts use chart-1 for the active line and chart-5 at low opacity for baselines, grids, and historical context.
- Chart hover cursor, active bar background, and tooltip highlight area use `--chart-hover`, one step above the background. Never use default white, light gray, or high-contrast hover bands.
- Depth via linework, spacing, typography, and restrained structural light, not shadow-heavy cards.
- No `#000000`. No `#FFFFFF`. Use defined tokens only.
- 60-30-10: 60% background, 30% surface+text, 10% accent.

Brand hue: `--brand-hue` is the single configurable knob for the accent system. `--primary`, `--primary-hl`, `--primary-glow`, and `--chart-2` through `--chart-5` each keep a fixed lightness and chroma and take only their hue from `--brand-hue`; `--chart-1` mirrors `--primary`. Changing `--brand-hue` rotates the whole accent system at once and preserves the lightness and chroma relationships that keep the ramp readable. The neutral base — backgrounds, surfaces, foregrounds, borders, `--chart-hover`, `--quote-bg`, `--outro-bg`, `--glow-ink` — stays fixed and does not track `--brand-hue`. Only `--brand-hue` is intended to be reconfigured; the lightness and chroma values are the fixed style recipe.

## 3. Typography

### Font loading

The following `@import` lines go at the very top of `index.css` before any other CSS. Without these imports, Satoshi, Geist Mono, and IBM Plex Mono fall back to system fonts and the Display Number loses its engineered typeface.

```css
@import url('https://api.fontshare.com/v2/css?f[]=satoshi@900,700,500,400&display=swap');
@import url('https://cdn.jsdelivr.net/npm/@fontsource/geist-mono@5.2.7/400.css');
@import url('https://cdn.jsdelivr.net/npm/@fontsource/geist-mono@5.2.7/500.css');
@import url('https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;500;600&display=swap');
@import url('https://cdn.jsdelivr.net/npm/misans@4.1.0/lib/Normal/MiSans-Regular.min.css');
@import url('https://cdn.jsdelivr.net/npm/misans@4.1.0/lib/Normal/MiSans-Medium.min.css');
@import url('https://cdn.jsdelivr.net/npm/misans@4.1.0/lib/Normal/MiSans-Semibold.min.css');
```

Font stacks:
- Primary: `'MiSans', 'Noto Sans SC', 'PingFang SC', 'Source Han Sans SC', sans-serif`
- Display Number: `'Satoshi', 'Geist Mono', sans-serif`
- Mono: `'IBM Plex Mono', ui-monospace, monospace`

Weight hierarchy (3 tiers): `font-normal` (400) Standard · `font-medium` (500) Heading/Display · `font-semibold` (600) Emphasis (delta indicator value and chapter marker number only). Display Number and title weights cap at `font-medium` (500). `font-bold` (700), `font-extrabold` (800), and `font-black` (900) are not used on Display Number, titles, or any large text.

Type scale (stair-step):

| Level | lg: Desktop | md: Tablet | Mobile (base) | Weight | LH | LS |
|-------|-------------|------------|---------------|--------|----|----|
| Display Number | `text-[200px]` | `text-[140px]` | `text-[88px]` | `font-medium` | `leading-[0.84]` | `tracking-[-0.06em]` |
| Section Primary | `text-[140px]` | `text-[96px]` | `text-[60px]` | `font-medium` | `leading-[0.86]` | `tracking-[-0.05em]` |
| Section Secondary | `text-[80px]` | `text-[56px]` | `text-[40px]` | `font-medium` | `leading-[0.90]` | `tracking-[-0.035em]` |
| Section Tertiary | `text-[48px]` | `text-[36px]` | `text-[28px]` | `font-normal` | `leading-[1.0]` | `tracking-[-0.02em]` |
| Page Title | `text-[56px]` | `text-[44px]` | `text-[36px]` | `font-medium` | `leading-[1.15]` | `tracking-[0]` |
| Quote | `text-[36px]` | `text-[28px]` | `text-[20px]` | `font-normal` | `leading-[1.45]` | `tracking-[0]` |
| Closing Title | `text-[44px]` | `text-[34px]` | `text-[26px]` | `font-normal` | `leading-[1.15]` | `tracking-[0.02em]` |
| Body | `text-[16px]` | `text-[16px]` | `text-[16px]` | `font-normal` | `leading-[1.65]` | `tracking-[0]` |
| Caption | `text-[13px]` | `text-[13px]` | `text-[13px]` | `font-normal` | `leading-[1.55]` | `tracking-[0]` |
| Meta/Label | `text-[11px]` | `text-[11px]` | `text-[10px]` | `font-medium` | — | `tracking-[0.14em]` |
| Eyebrow | `text-[10px]` | `text-[10px]` | `text-[10px]` | `font-medium` | — | `tracking-[0.14em]` |

Implementation: encode focal numbers with exact breakpoint values such as `text-[88px] md:text-[140px] lg:text-[200px]` with `leading-[N]` and `tracking-[Nem]` from the table, or explicit `fontSize`/`lineHeight`/`letterSpacing` switched at breakpoints. Avoid generic Tailwind size ramps for focal numbers. Avoid `clamp()` for `fontSize` on focal numbers — sub-pixel jitter from fluid scaling degrades the spring count-up animation. Minimum font size: `text-[10px]` absolute floor; required reading text uses `text-[14px]+` per §8 accessibility.

OpenType: `font-feature-settings: "cv01", "ss03", "tnum"` on all data numbers. Display Number uses Satoshi or Geist Mono with `tnum` for tabular alignment.

CJK: MiSans primary; weight matches the table above; `textWrap: "balance"` + `max-w-[28ch]` to `max-w-[42ch]`; 0.1em space flanking digits in mixed lines.

Descriptive copy: `text-[16px]` body / `text-[13px]` caption, `font-normal`, `leading-[1.55]` to `leading-[1.65]`. Use `--foreground-2` for explanatory prose. Use `--foreground-3` only for decorative or non-essential metadata. Any required reading text below `text-[14px]` uses `--foreground-2` or a lightened token that reaches 4.5:1 contrast. Align descriptions with the focal number or section grid, not centered by default. Max width: `max-w-[30ch]` to `max-w-[38ch]` for Chinese copy. No italic, no serif, no uppercase tracking on descriptive prose.

Hierarchy enforcement:
- One Primary number per section (largest + `--foreground`; accent `--primary` on the unit or delta only).
- Primary/Secondary/Tertiary differ by ≥ 2 scale tiers.
- Accent restricted to: unit text, delta indicator, chart-1, chapter marker, one structural accent element per section.

## 4. Spacing & Layout

Base unit: Tailwind spacing `2` (8px). Scale: `1` `2` `3` `4` `6` `8` `12` `16` `24` (Tailwind spacing units; `1` = 4px).

Section padding: `py-24 px-6` (mobile) → `py-40 px-16` (desktop).
Container: `max-w-5xl` centered. `gap-2` grid. `items-baseline` for mixed text. `min-w-0` on nested flex/grid children.

Overflow adaptation: long numbers and long labels must not overlap adjacent elements or break out of the section frame. Truncate, wrap, or scale text to fit; size every layout against the longest plausible value, not a short sample. Test focal numbers and labels at their maximum realistic length.

Density: one data story per viewport height.

Gaps: `gap-1` tight (indicator internals) · `gap-3` standard (label+number) · `gap-6` loose (intra-section) · section-level via padding.

Radius: `rounded-[0px]` sharp · `rounded-[2px]` standard · `rounded-[4px]` max for cards, panels, charts, tables, frames, and inline indicators. No `rounded-xl`, `rounded-2xl`, `rounded-3xl`, pill radius, blob card, or fully rounded card shape. Exception: pill elements (tags, badges) use full border-radius.

## 5. Material

Surface: **elevated** — depth via thin border + near-black surface contrast, not shadow. Panels use one-step surface lift (`--surface-l2` on `--background`), not stacked dark-on-dark.

Borders: Whisper `1px solid oklch(1 0 0 / 0.06)` · Strong `1px solid oklch(1 0 0 / 0.12)`.

Shadow: minimal — `inset 0 1px 0 oklch(1 0 0 / 0.03)` only. No heavy drop shadows or glow effects on cards.

Noise texture: subtle noise overlay on deep backgrounds for premium tactile quality. Noise is near-invisible (opacity ≤ 0.12, overlay blend mode).

Structural light: cold radial glow from corners or horizon — the primary ambient effect. Electric blue light as material, not decoration. Glow is a diffused background spotlight only, not a card edge or text effect.

Outlined pill: thin-border pill for categorical labels and status tags — `1px solid` at low accent opacity, transparent background, full border-radius.

Dividers: accent `linear-gradient(to right, transparent, oklch(0.700 0.160 var(--brand-hue) / 0.3), transparent) h-px`; content `oklch(1 0 0 / 0.08) h-px`.

Panel discipline: panels frame charts, dense tables, or grouped evidence only. Use sharp corners (`rounded-[2px]`) and thin borders. Each archetype carries one distinct material or spatial logic — instrument gauge, data rail, annotation margin, typographic field, or open negative space.

## 6. Motion

Engines: `motion/react` · `motion-plus/react` (AnimateNumber) · `@paper-design/shaders-react` (GodRays).

```ts
const EASE = {
  out:    [0.08, 0.95, 0.15, 1],
  inOut:  [0.86, 0, 0.10, 1],
  snap:   [0.12, 0, 0, 1],
  spring: [0.38, 0, 0.58, 1],
};
```

| Category | Duration | Easing |
|----------|----------|--------|
| Hover | 150ms | out |
| Button press | 100ms | snap |
| Element entrance | 500-900ms | out |
| Number rolling | 0.8-2.0s | spring physics, no visible bounce |
| Curve pathLength | 2.0-2.8s | out |
| Spotlight drift | 6s/cycle | inOut, infinite |
| WebGL | continuous | speed: 0.8 |

Entrance: Hero uses `animate=` with orchestrated delays. Other sections use `whileInView`, `once: true`, `margin: "-20%"`, and only animate the focal element. Section shell, headings, and readable text render visible before in-view animation; do not hide the whole section wrapper.

**whileInView is mandatory for scroll-triggered entrance — NEVER use useInView + animate.**
`motion/react` provides `whileInView` and `viewport` props that handle intersection observation internally. Do not manually create refs and `useInView` hooks to conditionally set `animate`.

```tsx
// ✅ CORRECT — whileInView + viewport (built-in, declarative)
<motion.div
  initial={{ opacity: 0, x: -20 }}
  whileInView={{ opacity: 1, x: 0 }}
  viewport={{ once: true, margin: "-20%" }}
  transition={{ duration: 0.7, ease: EASE.out, delay: 0.2 }}
>
```

Motion idea per viewport: one focal motion idea only. Hero staged reveal; Time Series path/fill draws; Proportion rail fills; Ranking rows cascade; Quote crossfades; Outro settles. Do not apply the same `y + opacity` reveal to every section.

AnimateNumber: per-digit scroll triggers on child value change — always use {displayValue || 0} (see §17). Only the Hero metric and one focal metric per section may scroll; supporting numbers render static. Motion: spring, no bounce, per-digit column. Unit text stays outside the animated number and is never duplicated.

SVG: `pathLength: 0→1` draw-in. Breathing via `d` morph. No `<circle>` inside `preserveAspectRatio="none"` — use HTML border-radius.

`prefers-reduced-motion`:
- CSS: `animation-duration: 0.01ms !important`
- Motion: section opacity crossfade only; no translate, scale, stagger, or path draw.
- WebGL: `speed: 0`
- AnimateNumber: plain final text

Data-driven value invariant: numerical animation primitives (AnimateNumber, useSpring numerical target, motion value transforms) need a meaningful value at each frame. When the data source returns null, undefined, or the query yields no rows, render the section's empty state (per §15) instead of attaching a broken animation.

Continuous motion: WebGL, SVG loops, CSS spotlight drift, and repeated background motion run only while their section is in view. Off-screen continuous motion pauses.

## 7. Iconography

`lucide-react`, 1–1.25px stroke, outline-only, `currentColor`. Sizing in rem, `gap-2` to text. Icons are never larger than the cap-height of the type they sit beside. Decorative: `aria-hidden="true"`. Semantic: `role="img"` + `aria-label`.

## 8. Accessibility & Mobile

WCAG 2.2 AA: ≥4.5:1 text contrast · ≥3:1 UI contrast · `size-6` (24×24px) targets, `size-11` (44×44px) mobile · `ring-2` focus ring · `prefers-reduced-motion` dual-layer · content reflows at 320px · logical heading hierarchy.

Small text contrast: required labels, captions, axes, and metadata below `text-[14px]` use `--foreground-2` or an equivalent 4.5:1 token. `--foreground-3` is for decorative marks, disabled states, and non-essential metadata only.

Breakpoints (mobile-first):

| Name | Width | Behavior |
|------|-------|----------|
| Base | < 768px | Single-column; full-width CTAs |
| md | ≥ 768px | Two-column layouts; padding increase |
| lg | ≥ 1024px | Full grids; sidebar visible |
| xl | ≥ 1440px | Content caps max-w-5xl; gutters absorb |

`min-h-dvh` for full-height. `env(safe-area-inset-bottom)` for sticky elements. Stair-step type scaling (Section 3 table).

## 9. Do's and Don'ts

Do:
- White focal numbers (`--foreground`) + electric blue signal (`--primary`) — numbers read, blue directs
- Structural light as ambient material — cold radial glow, not decorative neon
- Sharp edges, deep negative space, engineered geometric type
- Monochrome blue chart ramp (chart-1 through chart-5)
- One structural visual decision per viewport beyond color: crop, offset, scale, path, rail, field, light cone, or typographic lockup

- **Don't** render focal numbers in `--primary` (blue) — focal numbers use `--foreground` (white); `--primary` is a signal accent only
- **Don't** use warm tones, orange, or amber anywhere — this style is cold
- **Don't** use heavy shadows, frosted glass, `backdrop-blur`, or rim lighting — depth comes from linework, border, and structural light
- **Don't** use large-radius cards, `rounded-xl`, `rounded-2xl`, `rounded-3xl`, blob cards, or soft tiles — cards and panels use `rounded-[0px]` through `rounded-[4px]`
- **Don't** nest panels beyond one level — panels are flat, not stacked
- **Don't** use Inter, Geist Sans, Space Grotesk, or other overused Latin-only faces for body or labels — MiSans is the primary face; Satoshi and Geist Mono are for Display Number only
- **Don't** apply weight 600 globally — restricted to indicator values and marker numbers
- **Don't** use the same panel material for every section — vary material logic by archetype
- **Don't** fade whole sections with opacity-only — use a semantic focal motion such as path draw, proportion fill, row cascade, or subtle transform
- **Don't** tween numbers as single value — per-digit spring
- **Don't** expose rolling-number internals such as repeated `0123456789` stacks — fall back to the final static number
- **Don't** animate CSS layout properties (`width`, `height`, `padding`, `margin`, `top`, `left`, `right`, `bottom`, `font-size`, `gap`, `grid-template-*`) — animate `transform` and `opacity`; use FLIP only for unavoidable layout transitions
- **Don't** use ease strings — cubic-bezier arrays only
- **Don't** use `h-screen` — use `min-h-dvh`
- **Don't** use `useInView()` + conditional `animate={}` for entrance animations — use `whileInView` + `viewport` props. `useInView` is ONLY for controlling continuous motion (shader speed, SVG loop playback) that must pause off-screen.

```tsx
// ❌ Bad — manual useInView + conditional animate
const inView = useInView(ref);
<motion.div animate={inView ? "show" : "hidden"} />

// ✅ Good — whileInView + viewport
<motion.div initial="hidden" whileInView="show" viewport={{ once: true, margin: "-20%" }} />
```

- **Don't** use heavy / black weights (`font-bold` `font-extrabold` `font-black`, ≥ 700) on Display Number, titles, or any large text — overweight reads as clunky and loses precision. Display weight max is `font-medium` (500); `font-semibold` (600) is reserved for indicator values and marker numbers.
- **Don't** render any text below `text-[10px]` — 10px is the absolute floor; required reading text uses `text-[14px]+` per §8 accessibility.
- **Don't** wrap multiple sections in card containers — narrative density carries the page; cards belong inside one section as a grouped-evidence container, never as page-level scaffolding. **Don't** nest cards beyond a single Double-Bezel layer.
- **Don't** use drop shadows as a design feature — depth comes from linework, surface contrast, and structural light. Only the existing one-line inset highlight in §5 is permitted.

## 10. Pattern Overview

### Scenario Mindset

This page is a story told in data, not a dashboard — every section is a chapter, every chart is evidence for a claim.
The narrative arc runs across the whole page: opening hook → supporting evidence → closing impact. Each section advances exactly one beat of the arc.
Each section carries one focal claim and exactly one supporting visual (chart, ranking, or quote). Parallel KPI tiles, side-by-side metric grids, and three competing focal numbers belong to a dashboard, not a campaign report.
A successful page lets the reader leave with three things: the headline number, the why behind it, the so-what of it.
Archetype names are internal planning labels only. Visible eyebrows, captions, and labels come from the topic, period, data role, or business meaning, not from section archetype names.

### Scope

Scope: scrollable single-page data narrative where numbers are the protagonist.

Atmosphere:
- Information density: Low → High (rhythm-controlled)
- Visual rhythm: Strong alternation (different archetype per section)
- Narrative arc: Beginning → Middle → End

Supported data shapes: time-series · proportions · paired metrics · ranking · grouped metrics · event sequence · peer catalog · comparison.

### Voice & Copy

The page's copy reads like a curator's wall text — terse, third-person, observational.

- Voice: third-person observational. Use `we` only in attribution; never address the reader as `you`.
- Statements, not calls to action: `Bookings closed at 142,300`, not `We booked 142,300!` — a campaign report is a read-only artifact.
- Eyebrows: uppercase, mono, `tracking-[0.14em]`. Titles: sentence case, no terminal period. Body: sentence case, full sentences, terminal punctuation.
- Numbers in copy: tabular figures, thousands separators, no decimal padding.
- Period strings use a `·` middle dot (U+00B7) with single spaces around it.

## 11. Section Archetype Library

A section is composed from three independent choices: an **archetype** (what the section says), a **composition treatment** (how it is laid out), and a **dominant move** (the one element that leads the eye). Archetype and treatment are decoupled — the same archetype can take different treatments across a page, and that is where layout variety comes from. Treatment selection is rule-driven, not free: it follows the archetype's allowed list, the item count, and the adjacency rules in §12.

### 11.1 Composition Treatments

The layout-structure vocabulary. Each treatment describes composition logic only — grid and container structure, where the focal element sits, the density tier. Specific spacing, radius, and color values come from the style layer.

| Treatment | Composition logic | Density |
|-----------|-------------------|---------|
| **Full-bleed Monolith** | One element fills the frame, vertically centered; supporting elements stack around it. | spacious |
| **Asymmetric Split** | Two unequal columns (roughly 3:2 or 2:1). One column leads; the other carries supporting evidence or annotation. | medium |
| **Stacked Band** | Full-width horizontal bands stacked vertically; each band is one sub-claim. | medium–dense |
| **Matrix Grid** | Equal-size cells in an N×M grid. Narratively bound — every cell is evidence for this section's single claim, not a free-standing tile stack. | dense |
| **Proportion Rail** | One continuous rail, band, or scale carrying a parts-of-whole reading. | medium |
| **Offset Frame** | Content deliberately cropped, off-center, or breaking a frame edge. | medium |
| **Sequence Track** | A linear stepped progression; each step is index → title → note. | medium–dense |
| **Typographic Field** | Composition built from type alone — scale, weight, and spacing do the work, with no panels or cards. | spacious–medium |
| **Annotation Rail** | A main element paired with a narrow margin of notes or callouts. | medium |

### 11.2 Dominant Move

Each section leads with exactly one dominant move — the element that takes the eye first. Moves are either **steady** or **disruptive**; the page-level budget for disruptive moves is set in §12.

| Dominant Move | Class |
|---------------|-------|
| Scale jump — the focal element is ≥ 2 scale tiers larger than its surroundings | steady |
| Single stroke — one drawn line, accent stroke, or light cone leads the eye | steady |
| Path reveal — a curve draws in, or a proportion rail fills | steady |
| Crop / bleed — the focal element is cropped by an edge or overflows the frame | disruptive |
| Offset placement — the focal element sits deliberately off the regular grid position | disruptive |
| Column break — one element spans columns or goes full-width, interrupting the grid | disruptive |

### 11.3 Archetypes

Each archetype lists its trigger, slots, allowed treatments (with the selection rule), and dominant-move options. Archetype names are internal planning labels — visible eyebrows, captions, and labels come from the topic, period, data role, or business meaning, never from archetype names.

**Hero Monolith** — Trigger: opening anchor metric. Slots: eyebrow · metric-label · primary-value · unit-text · delta-indicator · subtitle. Treatments: Full-bleed Monolith **(default)** · Offset Frame · Typographic Field. Dominant Move: scale jump · crop/bleed · single stroke.

**Time Series** — Trigger: ordered values over a time dimension. Slots: title · time-axis · value-axis · series (1–3) · callout. Treatments: Stacked Band **(default)** (chart over a callout band) · Asymmetric Split (chart leads, reading on the side) · Annotation Rail. Dominant Move: path reveal · scale jump on the peak point.

**Proportion Field** — Trigger: parts-of-whole as percentages. Slots: proportion-visual · focal-value · integrated labels (2–5) · note. Treatments: Proportion Rail **(default)** · Stacked Band · Typographic Field. Dominant Move: path reveal (rail fill) · scale jump. Donut, pie, and ring charts are not used — proportion reads as a continuous rail or band.

**Comparison** — Trigger: two metrics contrasted. Slots: left-value · left-label · right-value · right-label · divider · delta. Treatments: Asymmetric Split **(default)** (weighted toward the leading value) · Stacked Band (stacked contrast) · Offset Frame. Dominant Move: scale jump · offset placement.

**Ranking / List** — Trigger: items ordered by magnitude. Slots: rank · label · value · delta · highlight (top 1–3). Treatments: Stacked Band **(default)** (≤ 5 items) · Matrix Grid (≥ 6 items, truncated past a readable count) · Asymmetric Split (top-1 isolated, the rest compressed). Dominant Move: scale jump on top-1 · offset placement · single stroke as a magnitude scale.

**Grouped Metric Cluster** — Trigger: several parallel metrics that together evidence one claim — for example a scenario's sub-metrics. Slots: cluster-claim · metric cells (2–6, each: label · value · note). Treatments: Matrix Grid **(default)** · Stacked Band. Dominant Move: scale jump (one metric leads) · single stroke. The cluster is narratively bound — one claim's evidence, not a free-standing dashboard tile stack (see §16).

**Sequence / Timeline** — Trigger: time-ordered events, milestones, or a process — distinct from Time Series, which carries values, not events. Slots: track · steps (each: index · title · note) · terminus. Treatments: Sequence Track **(default)** · Stacked Band. Dominant Move: path reveal · single stroke.

**Peer Set / Catalog** — Trigger: a set of same-kind peers shown together without a magnitude order — a product family, a set of brands. Slots: set-label · peer cells (each: name · attribute · optional value). Treatments: Matrix Grid **(default)** · Stacked Band · Asymmetric Split (one peer featured). Dominant Move: scale jump on a featured peer · crop/bleed. Narratively bound, same as Grouped Metric Cluster.

**Quote Interstitial** — Trigger: a narrative pause between data sections. Slots: mark · text · attribution. Treatments: Typographic Field **(default)** · Offset Frame. Dominant Move: column break · offset placement. Quote text is one sentence, 28 words or fewer.

**Outro Reverent** — Trigger: end of report. Slots: title · subtitle · flourish · statistical caveats (period, source, audit note where relevant). Treatments: Full-bleed Monolith **(default)** · Typographic Field. Dominant Move: single stroke · scale jump.

## 12. Composition

Default sequence: Hero Monolith → data-supported evidence chapters → optional Outro Reverent.

Section count: use as many sections as the data and business story support. Sparse datasets may produce Hero plus 1–2 evidence chapters. Stop before a section becomes filler. Omit archetypes whose data is absent.

### 12.1 Rhythm discipline

Four rules, all countable for self-check after generation:

1. **Density rotation** — every section is classified `dense`, `medium`, or `spacious` by atomic-element count (dense ≥ 6, medium 3–5, spacious ≤ 2). Adjacent sections differ in density; no two consecutive `dense` sections; a page uses all three tiers across its length.
2. **Treatment variety** — no two consecutive sections use the same composition treatment, and no two consecutive sections use the same archetype.
3. **Bold-move budget** — each section leads with exactly one dominant move. Disruptive moves (crop/bleed, offset placement, column break) stay near one in five sections; the rest lead with a steady move.
4. **Break cadence** — at least one section per page breaks the grid — a Full-bleed Monolith, an Offset Frame, or a Typographic Field. A page made only of regular grids and stacked bands reads as a template.

### 12.2 Other composition rules

Background rhythm: vary rhythm with composition, linework, and light placement rather than deep section bands.

Chapter markers: sequential 01, 02, 03… when useful. Markers are optional and vary by composition — side note, tick mark, rail label, divider number, or small stamp. **Don't** repeat the same circular stamp treatment across every chapter.

## 13. Section Anatomy

### 13.1 Hero Monolith

Focal: primary-value — the numeric value itself, rendered as the single largest element in the Hero at Display Number scale (per the style layer), and animated with the count-up roll. Secondary: delta-indicator, subtitle. Tertiary: eyebrow, metric-label, unit-text. The metric-label (the metric's name, e.g. a column display name) and the unit-text are supporting elements — never rendered at Display Number or Section scale, never larger than the primary-value.

Hero background field: a full-bleed shader, per the style layer. The Hero background is the shader only — do not place raster images or photographs in the Hero unless the user explicitly requests imagery.

Hero composition: the primary number interacts with at least one structural element from its treatment — a cropped edge, a spotlight cone, a stage horizon, an offset frame, or a data path. Avoid the plain centered big-number Hero made only from eyebrow, number, badge, and paragraph.

Primary-value selection — pick exactly one numeric value to headline the report. Resolve by field-name pattern, stopping at the first match:

1. **Rate / percentage field** — a field whose display name contains a percent or rate indicator. Show the average across all records.
2. **Explicit aggregate field** — a field whose display name is an aggregate keyword (total, aggregate, 总计, 合计). Show its sum.
3. **Primary metric field (max)** — otherwise, identify the primary metric field by name semantics — the field most likely to headline a report (revenue, GMV, score). Show its maximum.
4. **Primary metric field (avg)** — same field as rule 3, show the average instead.

Display the selected value directly. Preserve units and percent signs already present in the source value. If the selected value is missing or null, omit the Hero Monolith section entirely.

Eyebrow slot format: `{topic} · {period}`. Emit the actual period from data context; if period is unavailable, omit the period segment — do not emit placeholder strings.

### Hero animation contract

Hero entrance is orchestrated: a parent `motion.div` with `animate` (not `whileInView`) staggers children in sequence — eyebrow → primary-value → subtitle. AnimateNumber roll starts when its child value changes from 0 to the target.

```tsx
const heroVariants = {
  hidden: {},
  visible: { transition: { staggerChildren: 0.12 } },
};
const fadeUp = {
  hidden: { opacity: 0, y: 16 },
  visible: { opacity: 1, y: 0, transition: { duration: 0.7, ease: EASE.out } },
};

<motion.div initial="hidden" animate="visible" variants={heroVariants}>
  <motion.p variants={fadeUp}>{eyebrow}</motion.p>
  <motion.div variants={fadeUp}>
    <AnimateNumber transition={{ type: "spring", duration: 2.0, bounce: 0 }}>
      {displayValue || 0}
    </AnimateNumber>
  </motion.div>
  <motion.p variants={fadeUp}>{subtitle}</motion.p>
</motion.div>
```

AnimateNumber per-digit scroll triggers on child value change: `displayValue` starts at `0` (via `|| 0`), updates to target after data loads — this 0→target change starts the digit scroll. The Hero uses `animate` (not `whileInView`) because it is visible on load.

### 13.2 Non-Hero archetypes — slot mapping per treatment

For every section, the first slot is focal, the second is supporting evidence, labels and notes are tertiary. The table gives focal and secondary placement for the principal archetype × treatment combinations; combinations not listed follow the same logic from the treatment's structure in §11.1.

| Archetype × Treatment | Focal placement | Secondary placement | Favored move |
|-----------------------|-----------------|---------------------|--------------|
| Time Series · Stacked Band | chart fills the upper band | callout in the lower band | path reveal |
| Time Series · Asymmetric Split | chart in the wide column | reading + callout in the narrow column | path reveal |
| Proportion Field · Proportion Rail | the rail spans the section width | focal-value at the rail's dominant segment | path reveal |
| Proportion Field · Stacked Band | each band one proportion segment | integrated labels inline per band | scale jump |
| Comparison · Asymmetric Split | leading value in the wide column | trailing value + delta in the narrow column | scale jump |
| Comparison · Stacked Band | upper band the leading value | lower band the trailing value, divider between | offset placement |
| Ranking · Stacked Band | top-1 row, emphasized | rows 2–5 descending | scale jump on top-1 |
| Ranking · Matrix Grid | top-1 cell, emphasized | remaining cells in reading order | offset placement |
| Ranking · Asymmetric Split | top-1 isolated in the wide column | rest compressed in the narrow column | scale jump |
| Grouped Metric Cluster · Matrix Grid | the cluster-claim above the grid | metric cells equal-weight, one cell may lead | scale jump |
| Grouped Metric Cluster · Stacked Band | cluster-claim leads | metric bands stacked below | single stroke |
| Sequence / Timeline · Sequence Track | the track line | steps along it, index → title → note | path reveal |
| Peer Set / Catalog · Matrix Grid | set-label above the grid | peer cells equal-weight | scale jump on a featured peer |
| Peer Set / Catalog · Asymmetric Split | featured peer in the wide column | remaining peers in the narrow column | crop/bleed |
| Quote Interstitial · Typographic Field | the quote text, set large | mark and attribution flank it | column break |
| Outro Reverent · Full-bleed Monolith | the closing title, centered | subtitle and caveats below | single stroke |

## 14. Data → Section Mapping

| Data Shape | Archetype |
|------------|-----------|
| Report-level anchor metric | Hero Monolith |
| Time-ordered series | Time Series |
| Parts-of-whole | Proportion Field |
| Two contrasting values | Comparison |
| Ordered list by magnitude | Ranking / List |
| Parallel metrics, one claim | Grouped Metric Cluster |
| Time-ordered events or milestones | Sequence / Timeline |
| Same-kind peer set | Peer Set / Catalog |
| Narrative beat | Quote Interstitial |
| End reflection | Outro Reverent |

Priority: match data shape → check adjacency → select treatment per §11.3 allowed list → verify §12 rhythm rules.

## 15. Functional States

- **Loading**: skeleton replaces only data-dependent slots. Static section structure, Hero shader, and chapter markers remain visible.
- **Empty**: section frame + centered "No data available" in `--foreground-3`.
- **Error**: frame + retry icon + "Failed to load" + retry button in `--foreground-2`.

## 16. Do's and Don'ts

Do:
- Alternate archetypes between sections
- Keep backgrounds near-black; vary rhythm through composition and linework
- One focal element per section
- Skip data-absent sections
- Use chapter markers only where they support navigation or rhythm
- Use visible labels from topic, period, metric role, or business meaning

- **Don't** repeat the same archetype consecutively — breaks rhythm
- **Don't** show archetype names such as `Grouped Metric Cluster`, `Proportion Field`, `Ranking / List`, or `Outro Reverent` in visible UI — they are internal planning labels
- **Don't** use equal 3-column grids — data narratives need asymmetry
- **Don't** use a plain centered big-number Hero with pill indicator as the whole composition — compose the number with crop, path, light, or stage structure
- **Don't** fix section heights — let content determine with min-height
- **Don't** show multiple primary numbers in one section — dilutes focus
- **Don't** separate chart legends from their chart — same visual group
- **Don't** use donut, pie, or ring charts of any kind, or detached dot legends — proportion is always read as a continuous rail or band, with segments tinted in chart-ramp order
- **Don't** use sidecar KPI tile stacks — use evidence panels, annotations, thresholds, compact tables, or one support metric
- **Don't** repeat identical circular chapter markers across all sections — vary or omit markers
- **Don't** apply identical `y + opacity` section reveals everywhere — each archetype has one semantic motion idea
- **Don't** hide whole section wrappers before they enter view — headings and readable text remain visible for long-page capture
- **Don't** render white or light-colored overlays, masks, scrims, or loading skeletons — every overlay and skeleton uses dark surface or ink tokens; a white layer never appears in a dark-theme report
- **Don't** show more than 5 ranking items without truncation
- **Don't** allow horizontal scroll — stack on mobile
- **Don't** use emoji anywhere — not in copy, labels, indicators, captions, or bullets
- **Don't** add CTA buttons of any kind — no "view full data" / "view more" / "view detail" / "查看完整", no action buttons, no links styled as buttons. Campaign reports are read-only narrative artifacts. Reject dashboard verbs (track, monitor, drill down, explore), marketing verbs (unlock, discover, supercharge, transform), and second-person imperatives.
- **Don't** force an outro when the data has no closing claim — end on the strongest supported evidence section
- **Don't** use the same composition treatment in two consecutive sections — vary the treatment, not only the archetype
- **Don't** keep a whole page at `medium` density — without a compression-and-breath rhythm the page reads flat
- **Don't** build a page with zero break sections — at least one section breaks the grid (Full-bleed, Offset Frame, or Typographic Field)
- **Don't** give every section a heading — space and rhythm can carry a content shift without a label
- **Don't** ship a perfectly symmetric layout with no visual tension — alternating density reads as intentional
- **Don't** lead a section with more than one dominant move — the protagonist is singular
- **Don't** render a Matrix Grid or Grouped Metric Cluster that is not narratively bound — a grid of cells is valid only as one claim's evidence, never as a page-wide parallel tile stack

## 17. Components Inventory

### Pre-imported

**GodRays** (@paper-design/shaders-react) — Cold radial WebGL light for Hero structural illumination. Usage: full-bleed shader layer behind Hero content creating asymmetric directional glow. Pair with CSS radial-gradient for layered depth and SVG noise for tactile grain texture.
```tsx
import { GodRays } from "@paper-design/shaders-react";

<GodRays
  speed={isShaderActive ? 0.8 : 0}
  colorBack="#0c0a14"
  colorBloom="#4654f0"
  colors={["#4654f050", "#2a38c0d0", "#ffffff10", "#14122000"]}
  density={0.12}
  spotty={0.08}
  midIntensity={0.25}
  midSize={0.15}
  intensity={0.45}
  bloom={0.3}
  offsetX={1}
  fit="cover"
  style={{ position: "absolute", inset: 0 }}
/>
```
`isShaderActive = inView && !prefersReducedMotion`. `offsetX={1}` positions the light source at top-right. Total dark overlay opacity ≤ 0.35.

Noise texture overlay: SVG `feTurbulence` for film-grain quality. Use a unique filter ID per section.
```tsx
<svg className="absolute inset-0 w-full h-full pointer-events-none" style={{ opacity: 0.1, mixBlendMode: "overlay" }} aria-hidden>
  <filter id="noise-grain">
    <feTurbulence type="fractalNoise" baseFrequency="0.65" numOctaves="3" stitchTiles="stitch" />
  </filter>
  <rect width="100%" height="100%" filter="url(#noise-grain)" />
</svg>
```

Hero background composition: GodRays (structural light) + CSS radial-gradient (corner glow) + SVG noise (grain texture), three layers:
```tsx
<div className="absolute inset-0 overflow-hidden" aria-hidden>
  <GodRays
    speed={isShaderActive ? 0.8 : 0}
    colorBack="#0c0a14"
    colorBloom="#4654f0"
    colors={["#4654f050", "#2a38c0d0", "#ffffff10", "#14122000"]}
    density={0.12}
    spotty={0.08}
    midIntensity={0.25}
    midSize={0.15}
    intensity={0.45}
    bloom={0.3}
    offsetX={1}
    fit="cover"
    style={{ position: "absolute", inset: 0 }}
  />
  <div style={{
    position: "absolute", inset: 0,
    background: `
      radial-gradient(ellipse at 80% 15%, oklch(0.620 0.180 var(--brand-hue) / 0.28), transparent 55%),
      radial-gradient(ellipse at 20% 85%, oklch(0.500 0.120 var(--brand-hue) / 0.15), transparent 50%)
    `
  }} />
  <svg className="absolute inset-0 w-full h-full pointer-events-none" style={{ opacity: 0.1, mixBlendMode: "overlay" }}>
    <filter id="hero-noise">
      <feTurbulence type="fractalNoise" baseFrequency="0.65" numOctaves="3" stitchTiles="stitch" />
    </filter>
    <rect width="100%" height="100%" filter="url(#hero-noise)" />
  </svg>
</div>
```

**AnimateNumber** (motion-plus/react) — Per-digit scroll (odometer) for the Hero metric and one focal metric per section. Each digit column scrolls independently to its target. The Hero primary-value and one focal metric per evidence section wrap their numeric display in AnimateNumber.
```tsx
import { AnimateNumber } from "motion-plus/react";

<AnimateNumber transition={{ type: "spring", duration: 2.0, bounce: 0 }}>
  {displayValue || 0}
</AnimateNumber>
```
`|| 0` ensures initial render is 0; the 0→target value change triggers the per-digit scroll. Do not append a duplicate percent sign — source values already carry units. Reduced motion: render plain final text.

**Recharts** (recharts) — AreaChart, BarChart, LineChart for time-series and category visualization. Styling rules:
- `isAnimationActive={false}` on every chart — recharts default animation conflicts with motion entrance
- Series colors: explicit `var(--chart-1)` through `var(--chart-5)`, never library defaults or Tailwind palette
- Tooltip cursor / active bar / hover: `var(--chart-hover)`, never default white or light gray
- Axis stroke and tick text: `var(--foreground-2)`; gridlines: `var(--foreground-3)`
- `fillOpacity={0.2}` on Area fills

### Inline-injected

**DeltaIndicator** — Compact change indicator with prominent directional arrow. Usage: any place a metric needs to communicate "how much it changed" alongside the absolute value.
```tsx
function DeltaIndicator({ value, direction, label }: { value: string; direction: "up" | "down"; label?: string }) {
  const isUp = direction === "up";
  const Arrow = isUp ? "↑" : "↓";
  const tone = isUp
    ? { background: "oklch(0.620 0.180 var(--brand-hue) / 0.06)", border: "1px solid oklch(0.620 0.180 var(--brand-hue) / 0.45)", color: "var(--primary)" }
    : { background: "oklch(0.500 0.020 262 / 0.05)", border: "1px solid oklch(0.580 0.012 255 / 0.4)", color: "var(--foreground-2)" };
  return (
    <span className="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-[4px] text-[14px] font-medium" style={tone}>
      <span className="text-[18px] font-semibold leading-none" aria-hidden>{Arrow}</span>
      <span className="font-semibold">{value}</span>
      {label ? <span className="opacity-70 font-normal">{label}</span> : null}
    </span>
  );
}
```

**SpotlightGradient** — Radial halo overlay creating directional ambient light. Usage: layer behind a section's main content for a subtle directional glow; one per section max.
```tsx
function SpotlightGradient({ position, intensity = 0.7 }: { position: "top-center" | "top-right" | "bottom-center"; intensity?: number }) {
  const map = { "top-center": "50% 0%", "top-right": "80% 10%", "bottom-center": "50% 100%" } as const;
  const bg = `radial-gradient(ellipse at ${map[position]}, oklch(0.700 0.160 var(--brand-hue) / ${intensity * 0.25}), transparent 60%)`;
  return <div className="absolute inset-0 pointer-events-none" style={{ background: bg }} aria-hidden />;
}
```

**ChapterStamp** — Optional SVG circular marker variant with a sequential number. Usage: use sparingly when a circular stamp fits the composition; otherwise use a side note, tick mark, rail label, or divider number.
```tsx
function ChapterStamp({ number }: { number: string }) {
  return (
    <svg width="56" height="56" viewBox="0 0 56 56" aria-hidden>
      <circle cx="28" cy="28" r="26" fill="none" stroke="oklch(0.620 0.180 var(--brand-hue) / 0.4)" strokeWidth="1" />
      <text x="28" y="33" textAnchor="middle" fontSize="14" fontFamily="'IBM Plex Mono', monospace" fill="var(--primary)">{number}</text>
    </svg>
  );
}
```

**OutlinedPill** — Thin-border pill for categorical labels and status tags. Usage: lightweight classification label within a section.
```tsx
function OutlinedPill({ children }: { children: React.ReactNode }) {
  return (
    <span
      className="inline-flex items-center px-3 py-1 rounded-full text-[12px] font-medium tracking-wide"
      style={{
        border: "1px solid oklch(0.620 0.180 var(--brand-hue) / 0.35)",
        color: "var(--foreground-2)",
        background: "transparent"
      }}
    >
      {children}
    </span>
  );
}
```

## 18. Component Constraints

Use top-level hooks, typed props, `motion/react`, `motion-plus/react`, numeric easing arrays, `aria-hidden` for decorative visuals, semantic labels for readable values, `isAnimationActive={false}` on Recharts, and reduced-motion fallbacks for AnimateNumber, WebGL, spotlight, and off-screen continuous motion.
